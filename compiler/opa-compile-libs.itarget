# This file lists all libs that are needed to COMPILE opa source
# using the opa compiler.
# Only their runtime should be needed to compile servers.

# Caution: used by many scripts, in particular, make_distrib.sh

database.cmxa
libbase.cmxa
libtrx.cmxa
qml2ocaml.cmxa
qmlflatcompiler.cmxa
opalib.cmxa
opabsl_for_compiler.cmxa
